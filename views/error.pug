extends layout

block content
  div(style="text-align:center")
    h1 Whoops! Looks like something went wrong!
    img.error-img(src='../static/imgs/error.png')
    h3.error= `${error.status}: ${error.message}`
    nav.grid-container.portfolio-breadcrumb
      a(href="/") ← Go Home
    pre.stack.trace= error.stack
